{
  "name": "seo-dungeon",
  "version": "1.9.0",
  "description": "A 16-bit dungeon crawler interface for Claude SEO audits",
  "scripts": {
    "start": "node launch.js",
    "dev": "concurrently \"npm run server\" \"npm run game\"",
    "server": "node server/index.js",
    "game": "vite",
    "build": "vite build",
    "serve": "npx serve dist -l 3000 -s"
  },
  "dependencies": {
    "concurrently": "^9.1.2",
    "express": "^4.21.2",
    "phaser": "^3.87.0",
    "playwright": "^1.58.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vite": "^6.3.0"
  }
}
