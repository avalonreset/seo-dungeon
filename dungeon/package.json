{
  "name": "seo-dungeon",
  "version": "2.2.0",
  "description": "A Codex-default 16-bit dungeon crawler interface for SEO audits",
  "scripts": {
    "start": "node launch.js",
    "dev": "concurrently \"npm run server\" \"npm run game\"",
    "server": "node server/index.js",
    "game": "vite",
    "build": "vite build",
    "serve": "npx serve dist -l 3000 -s"
  },
  "dependencies": {
    "concurrently": "^10.0.3",
    "express": "^5.2.1",
    "phaser": "^3.87.0",
    "playwright": "^1.58.2",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
